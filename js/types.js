export const DEV = true

/**
 * Draw the array of pixels that produce a two digit number 
 * @callback drawPixels
 * @param {number[][]} px
 * @returns {void}
 */

/**
 * ClockNumber type - represents arrays of dots that    
 * form a two digit number at location {x, y}
 * @typedef ClockNumber
 * @type {object}
 * @property {number} x - the horizontal position
 * @property {number} y - the vertical position
 * @property {drawPixels} [drawPixels]
 */
